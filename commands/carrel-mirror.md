---
description: Synthesize your research patterns from reflections, capability log, and friction log
argument-hint: "--write --from-stdin (skill synthesizes content + pipes via stdin)"
---
!carrel vault mirror ${ARGS}
