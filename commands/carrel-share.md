---
description: Generate a vault-specific onboarding handbook for a collaborator (RA, co-author, lab member)
argument-hint: generate --mode <mode> --for <name> --sensitivity <level>
---
!carrel vault share ${ARGS}
