---
description: Quick end-of-session reflection — what worked, what didn't, what to improve
argument-hint: "--append --from-stdin (skill conducts reflection + pipes via stdin)"
---
!carrel vault reflect-log ${ARGS}
