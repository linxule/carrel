---
description: Set up or update overnight vault maintenance and analytical tasks
argument-hint: configure --enabled <bool> --trust-level <level> --schedule <cron> --review-cadence <period>
---
!carrel automate ${ARGS}
