---
description: Add or review multi-model agent teammates — Codex (ChatGPT), Gemini, Kimi
---

# /carrel-teammates

Delegates to the `model-teammates` skill. Records status in `model_teammates` on `.carrel/environment.json` and regenerates the dashboard.

Use anytime — standalone post-setup, or invoked during `/carrel-setup` Phase 5b.

**Skill**: `skills/model-teammates/SKILL.md`
**Spec**: `planning/specs/013-model-teammates.md`
