# 0.7.0 → 0.7.1

Patch release. No vault-side action needed.

## What changed

- **Removed `src/carrel/trust.py` backward-compat shim** (deferred from v0.8.0). The shim was a one-line re-export of `carrel.policy.trust` introduced during spec 010's namespace move in v0.7.0. Verified zero internal imports across the tree before removal; no third-party consumers possible since the shim shipped only hours earlier.

## Imports to update (only relevant if you wrote external code against v0.7.0 in the past few hours)

```python
# Before
from carrel.trust import is_allowed, ACTIONS, TRUST_HIERARCHY

# After
from carrel.policy.trust import is_allowed, ACTIONS, TRUST_HIERARCHY
```

The CLI (`carrel trust check|list|show`) is unchanged.

## Tests

228 passing. Removal verified by full test run.
